pyttsx3>=2.90            # offline TTS (Windows SAPI5, macOS NSSpeech, eSpeak on Linux)
SpeechRecognition>=3.10  # optional: --voice input
# PyAudio is only needed for --voice; on Windows: pip install pipwin && pipwin install pyaudio
pyaudio>=0.2.14; sys_platform != "win32"
tomli>=2.0; python_version < "3.11"
