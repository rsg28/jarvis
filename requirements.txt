# Text-to-speech
edge-tts>=6.1            # Microsoft Edge neural voices (free, no API key)
pygame>=2.5              # audio playback for edge-tts output
pyttsx3>=2.90            # fallback: offline SAPI5 / NSSpeech / eSpeak

# Voice input (--voice flag)
SpeechRecognition>=3.10
pyaudio>=0.2.14

# News + soccer skills
feedparser>=6.0          # Google News RSS
requests>=2.31           # ESPN scoreboard, JokeAPI, OpenTDB

# System + media control
psutil>=5.9              # battery / cpu / ram / disk info
pycaw>=20240210          # Windows volume set/get via IAudioEndpoint
comtypes>=1.2            # COM bindings for pycaw
Pillow>=10.0             # screenshots (PIL.ImageGrab)

# Clap-to-launch watcher
sounddevice>=0.5         # low-latency mic stream for clap detection
numpy>=1.26              # amplitude analysis

# Global hotkey (push-to-talk mode)
keyboard>=0.13           # system-wide keyboard shortcuts, no admin needed

# Speaker verification (only responds to enrolled voice) — optional.
# Uncomment and install if you want it. Order matters on Windows:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install webrtcvad-wheels
#   pip install --no-deps resemblyzer
#   pip install librosa Unidecode inflect
# (The `webrtcvad-wheels` package ships prebuilt Windows binaries so you
# don't need Microsoft C++ Build Tools.)
# torch>=2.2
# webrtcvad-wheels>=2.0.14
# resemblyzer>=0.1.4
# librosa>=0.10
# Unidecode>=1.3
# inflect>=7.0

# TOML config on Python 3.10 and older
tomli>=2.0; python_version < "3.11"
PySide6>=6.6
